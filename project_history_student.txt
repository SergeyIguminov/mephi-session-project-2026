    1  nmcli connection show
    2  ip a
    3  sudo hostnamectl set-hostname mephi-2026.domain.local
    4  hostnamectl
    5  sudo nmcli connection modify "Проводное подключение 1" ipv4.addresses 192.168.0.100/24
    6  sudo nmcli connection modify "Проводное подключение 1" ipv4.gateway 192.168.0.1
    7  sudo nmcli connection modify "Проводное подключение 1" ipv4.dns "8.8.8.8"
    8  sudo nmcli connection modify "Проводное подключение 1" ipv4.method manual
    9  sudo nmcli connection modify "Проводное подключение 1" ipv4.ignore-auto-dns yes
   10  sudo nmcli connection down "Проводное подключение 1"
   11  sudo nmcli connection up "Проводное подключение 1"
   12  ip addr show
   13  ip route | grep default
   14  ping -c 4 8.8.8.8 > /tmp/network_check.txt
   15  ping -c 4 192.168.0.1 >> /tmp/network_check.txt
   16  cat /tmp/network_check.txt
   17  sudo dnf makecache
   18  sudo dnf install -y nginx tcpdump libcap-ng-utils
   19  rpm -qa | grep -E "nginx|tcpdump|libcap-ng"
   20  dnf download --destdir=/tmp tcpdump
   21  sudo rpm -Uvh /tmp/tcpdump-*.rpm
   22  tcpdump --version
   23  lsblk
   24  sudo fdisk /dev/sdb
   25  sudo mkfs.ext4 -L MEPHI_DATA /dev/sdb1
   26  sudo mkdir -p /data/mephi-web
   27  echo "LABEL=MEPHI_DATA /data/mephi-web ext4 defaults 0 2" | sudo tee -a /etc/fstab
   28  sudo mount -a
   29  df -h | grep mephi-web
   30  sudo systemctl start nginx
   31  sudo systemctl enable nginx
   32  systemctl status nginx
   33  sudo journalctl -u nginx --since "5 minutes ago" > /tmp/nginx_recent_logs.txt
   34  cat /tmp/nginx_recent_logs.txt
   35  sudo groupadd mephi-devs
   36  sudo useradd -m -G mephi-devs mephi-admin
   37  echo "mephi-admin:P@ssw0rd2026" | sudo chpasswd
   38  sudo visudo
   39  sudo -l -U mephi-admin
   40  sudo chown mephi-admin:mephi-devs /data/mephi-web
   41  sudo chmod 2775 /data/mephi-web
   42  ls -ld /data/mephi-web
   43  sestatus
   44  sudo semanage fcontext -a -t httpd_sys_content_t "/data/mephi-web(/.*)?"
   45  sudo restorecon -Rv /data/mephi-web
   46  ls -Z /data/mephi-web
   47  sudo chmod u-s /usr/sbin/tcpdump
   48  sudo setcap cap_net_raw,cap_net_admin+ep /usr/sbin/tcpdump
   49  getcap /usr/sbin/tcpdump
   50  sudo -u mephi-admin tcpdump --help
   51  echo "root" | sudo tee /etc/security/denied_users
   52  sudo chmod 644 /etc/security/denied_users
   53  echo "auth required pam_listfile.so item=user sense=deny file=/etc/security/denied_users onerr=succeed" | sudo tee -a /etc/pam.d/sshd
   54  echo "auth required pam_listfile.so item=user sense=deny file=/etc/security/denied_users onerr=succeed" | sudo tee -a /etc/pam.d/login
   55  sudo systemctl restart sshd
   56  sudo grep pam_listfile /etc/pam.d/sshd /etc/pam.d/login
   57  su - mephi-admin
   58  history > ~/project_history_student.txt
